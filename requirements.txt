# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# LangChain
langchain==0.3.14
langchain-openai==0.3.0
langchain-core==0.3.31
langchain-community==0.3.14

# Data validation
pydantic==2.10.4

# HTTP client (sync + async)
httpx==0.28.1

# Environment
python-dotenv==1.0.1

# Observability
langsmith==0.2.10

# Vector search (local catalog RAG)
faiss-cpu==1.12.0
sentence-transformers==3.3.1
langchain-huggingface==0.1.2

# Session store (Redis — optional, falls back to in-memory if not set)
redis[hiredis]==5.2.1

# PostgreSQL async (leads persistence)
asyncpg==0.30.0
